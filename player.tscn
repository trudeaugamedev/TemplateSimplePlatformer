[gd_scene load_steps=5 format=3 uid="uid://dmjs1mntieaic"]

[ext_resource type="Texture2D" uid="uid://cjctiv8k1c008" path="res://Images/Player.png" id="1_rf3bp"]

[sub_resource type="GDScript" id="GDScript_jc3yj"]
script/source = "extends CharacterBody2D


const SPEED = 300.0
const JUMP_VELOCITY = -400.0


func _physics_process(delta: float) -> void:
	# Add the gravity.
	if not is_on_floor():
		velocity += get_gravity() * delta

	# Handle jump.
	if Input.is_action_just_pressed(\"ui_accept\") and is_on_floor():
		velocity.y = JUMP_VELOCITY

	# Get the input direction and handle the movement/deceleration.
	# As good practice, you should replace UI actions with custom gameplay actions.
	var direction := Input.get_axis(\"ui_left\", \"ui_right\")
	if direction:
		velocity.x = direction * SPEED
	else:
		velocity.x = move_toward(velocity.x, 0, SPEED)

	move_and_slide()
"

[sub_resource type="RectangleShape2D" id="RectangleShape2D_68cqn"]
size = Vector2(46, 106)

[sub_resource type="SpriteFrames" id="SpriteFrames_aeq82"]
animations = [{
"frames": [{
"duration": 1.0,
"texture": ExtResource("1_rf3bp")
}],
"loop": true,
"name": &"default",
"speed": 5.0
}]

[node name="Player" type="CharacterBody2D"]
scale = Vector2(0.48, 0.48)
script = SubResource("GDScript_jc3yj")

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
position = Vector2(1, 5)
shape = SubResource("RectangleShape2D_68cqn")

[node name="AnimatedSprite2D" type="AnimatedSprite2D" parent="."]
scale = Vector2(0.2925, 0.2925)
sprite_frames = SubResource("SpriteFrames_aeq82")
